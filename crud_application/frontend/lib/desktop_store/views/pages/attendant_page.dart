import 'package:flutter/material.dart';

class AttendantPage extends StatelessWidget {
  const AttendantPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Atendente'),
      ),
      body: Container(),
    );
  }
}
